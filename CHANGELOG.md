# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따릅니다.

## [Unreleased]

### Added
- **쇼핑몰(관리자): 반품 환불을 반품 화면에서 바로 실행합니다.** 그동안 환불은 주문 상세에서만 할 수 있어 화면을 오가야 했고, 무엇보다 **그 환불이 어느 반품 건의 것인지 기록에 남지 않았습니다.** 한 주문에 반품이 둘이면 구분할 수 없었고, 실제로 환불하지 않고도 반품을 완료로 찍을 수 있었으며, 예정액과 다른 금액을 환불해도 아무도 막지 않았습니다. 이제 `환불대기` 상태의 반품 화면에서 **환불 방법만 고르면 환불 예정액이 그대로 환불되고, 성공한 뒤에야 반품이 완료됩니다.** 금액은 입력받지 않습니다 — 접수 때 계산해 둔 예정액으로 고정하는 편이 어긋난 금액이 나가는 길을 막습니다. 환불 기록은 그 클레임에 귀속되며(`shop_payment_transactions.claim_id`, 마이그레이션 034) 화면에 `이 반품 건으로 N원이 이미 환불되었습니다`로 표시됩니다. 시스템 밖에서 이미 환불한 경우를 위해 `환불 없이 완료로 표시`를 사유 입력과 함께 남겨 두었습니다. 주문 상세의 환불 처리는 그대로입니다 — 반품과 무관한 환불은 클레임에 귀속되지 않는 것이 맞습니다
- **쇼핑몰: 고객이 반품을 직접 신청할 수 있고, 반품도 교환과 같은 회수·검수를 거칩니다.** 지금까지 반품은 고객 화면에 아예 없어서 전화·문의로 요청하면 관리자가 대신 등록했고, 뒤쪽도 "요청 → 승인" 두 단계뿐이라 **승인 즉시 반품완료가 찍혔습니다** — 물건을 받기 전에 완료 처리되고, 고객이 보냈는지 우리가 받았는지 구분할 자리도 없었습니다. 이제 반품은 교환과 같은 클레임 워크플로우를 씁니다: `신청 → 승인 → 회수중 → 회수완료 → 검수중 → 환불대기 → 반품완료`. 검수에서 거절하면 교환과 똑같이 `고객 반송 → 종결`로 갑니다. **환불은 자동 실행하지 않습니다** — 워크플로우가 `환불대기`까지 데려다 놓고, 관리자가 주문 상세의 환불 처리에서 환불한 뒤 `환불 완료`로 확정합니다(교환의 '교환비 납부 확인'과 같은 방식). 돈이 나가는 동작을 상태 변경에 딸려 자동으로 실행하지 않기 위해서입니다
- **쇼핑몰: 반품·교환을 수량 단위로 나눠 신청할 수 있습니다.** 3개를 받았는데 2개는 불량이라 교환하고 1개는 필요 없어져 반품하는 일은 실제로 일어납니다. 이제 한 상품에 여러 건의 클레임이 붙을 수 있고, 서로 다른 유형도 섞을 수 있습니다(겹침은 수량으로 막습니다 — 이미 잡힌 수량과 신청 수량의 합이 주문 수량을 넘으면 거부). 신청한 뒤에도 **남은 수량이 있으면 버튼이 그대로 남아** 이어서 신청할 수 있습니다. 이전에는 클레임이 하나라도 있으면 버튼이 사라져 나머지를 신청할 방법이 없었습니다. 고객 주문 상세에는 상품 아래에 `교환 2개 · 상품 불량 · 교환신청` 처럼 건별로 한 줄씩 나오고, 관리자 주문 상세도 건별로 표시됩니다(이전에는 마지막 한 건만 남아 유형과 수량이 어긋나 보였습니다)
- **쇼핑몰: 전량 반품이 끝난 상품은 구매확정할 수 없습니다.** 반품이 완료돼도 주문상품 상태가 `배송완료`로 남아, 돌려보낸 상품을 고객이 구매확정할 수 있었습니다. 이제 완료된 반품 수량이 주문 수량 전체를 덮으면 주문상품도 `반품완료`로 옮깁니다. 부분 반품은 남은 수량이 있으므로 종전대로 둡니다
- **쇼핑몰: 반품·교환 사유가 코드가 아니라 말로 나옵니다.** 관리자 화면에 `CHANGE_MIND`·`DEFECT` 가 그대로 보이던 자리에 `단순 변심`·`상품 불량` 이 나오고, 클레임 목록에 사유 열이 생깁니다. 선택지가 프론트 모달·관리자 모달·서버 검증 세 곳에 따로 하드코딩돼 문구까지 달랐던 것을 `ClaimReason` 하나로 모았습니다. 이 값은 라벨이 아니라 **비용 부담을 정합니다** — 불량·오배송·배송지연은 판매자 귀책이라 교환비·반품비를 물리지 않으며, 그 규칙도 같은 곳이 소유합니다
- **쇼핑몰: 주문 상세의 교환 버튼이 `반품·교환` 하나로 합쳐집니다.** 고객이 하려는 일은 "이 상품에 문제가 있다" 하나이고 반품·교환은 그 해결 방식이므로, 버튼 하나로 들어가 모달 안에서 고릅니다. **교환 옵션이 없는 상품(단종·품절 등)도 이제 반품은 신청할 수 있습니다** — 그동안은 바꿔 줄 옵션이 없으면 버튼 자체가 나오지 않아 고객이 할 수 있는 일이 없었습니다. 그 경우 모달에서 교환은 잠기고 이유를 밝힙니다. 반품을 고르면 회수 후 확인해 환불한다는 것과, 고객 귀책 시 반품 배송비가 환불액에서 차감된다는 것을 그 자리에서 안내합니다
- **쇼핑몰: 운송장을 배송 묶음별로 등록할 수 있습니다.** 배송비를 따로 받은 묶음은 출고 정책이 다르다는 뜻이고(묶음마다 반품지가 따로 저장됩니다), 반품지가 다르면 물건이 나가는 곳도 다릅니다. 배송지가 하나여도 묶음이 둘이면 송장은 둘인데, 지금까지 운송장 등록은 주문 단위여서 어느 송장이 어느 상품을 싣고 있는지 지정할 방법이 없었습니다. 이제 주문 상세의 운송장 등록에 묶음 선택이 생기고(묶음이 둘 이상인 주문에만 나옵니다), 그 묶음의 상품만 배송 상태가 움직입니다. 이미 송장이 붙은 묶음은 선택지에 표시되므로 무엇이 남았는지 목록을 다시 훑지 않아도 됩니다. 고객 주문 상세에도 운송장마다 어떤 상품이 실렸는지 나오므로 "왜 일부만 왔지"가 의문으로 남지 않습니다. **`shop_shipments` 에 `shipping_group_key` 컬럼이 추가됩니다(마이그레이션 031).** 기존 운송장은 값이 비어 있고 주문 전체를 싣는 것으로 해석하므로 동작이 달라지지 않습니다
- **에디터: 워드·엑셀 문서를 본문으로 가져올 수 있습니다.** 문서 가져오기에 DOCX·XLSX 가 더해집니다 — 워드는 문단·제목·굵게/기울임·목록·표를, 엑셀은 첫 시트를 표로 옮깁니다. 서버에 추가로 설치할 것은 없습니다(PHP 내장 기능만 씁니다). **PDF 는 아직 지원하지 않습니다** — 텍스트 추출에 외부 프로그램(poppler)이 필요해서 설치된 서버에서만 되는 기능이 되기 때문입니다. 파일 선택 창에는 PDF 도 보이지만 고르면 지원하지 않는다고 알려 줍니다
- **에디터: 링크를 붙여넣으면 미리보기 카드로 넣을 수 있습니다.** 주소 하나만 붙여넣었을 때 나오는 선택지에 "OG 카드"가 더해집니다 — 서버가 해당 페이지의 제목·설명·대표이미지를 읽어 카드로 만듭니다. 읽어 오지 못하면 단순 링크로 넘어갑니다. 결과는 하루 동안 캐시하므로 같은 링크를 여러 번 붙여넣어도 매번 나가지 않습니다
- 에디터: 두 기능의 API(`POST /api/v1/editor/convert`, `GET /api/v1/editor/og`)는 인증 없이 부를 수 있습니다 — 비회원 글쓰기에서도 에디터를 쓰기 때문이며 이미지 업로드 라우트와 같은 판단입니다. 대신 IP 단위로 남용을 제한합니다(변환 10분 20회, 링크 메타 10분 30회). 링크 메타는 서버가 사용자가 준 주소로 요청을 내보내므로, 사설·루프백·링크로컬·예약 대역을 막고 검사한 IP 로 못을 박아 접속하며(DNS 리바인딩 차단) 리다이렉트도 매 단계 다시 검사합니다. 업로드한 문서는 저장하지 않고, 외부 엔티티(XXE)와 압축 폭탄도 막습니다
- **에디터: MubloEditor 를 1.7.1 로 올렸습니다.** 인용구 스타일 갤러리(기본·컬러·아이콘·알림박스·특수 34종), 체크리스트(체크 상태가 본문에 남아 글 보기에서도 유지됩니다), 목차 삽입(H1~H3 를 훑어 앵커와 함께 넣고, 이미 있으면 교체합니다), 슬래시 커맨드(빈 줄에서 `/` 를 치면 삽입 메뉴), 스마트 붙여넣기(링크 하나를 붙여넣으면 썸네일 카드·플레이어 임베드·단순 링크 중에서 고릅니다. 이번 선택을 세션 동안 기억할 수 있습니다), 표 안에서 Tab 으로 셀 이동(마지막 셀에서 Tab 은 행 추가)과 셀 패딩·간격·테두리·배경색을 다루는 표 스타일 창이 생깁니다. 저장된 기존 본문은 그대로 열립니다
- 에디터: 툴바 버튼과 모달에 접근성 표시가 붙습니다 — 툴바 `role`, 버튼 `aria-label`, 모달 `role="dialog"`·`aria-modal` 과 Tab 포커스 가둠. 키보드로 모달을 열면 포커스가 모달 안에서 순환하고 ESC 로 닫힙니다
- **에디터: 공식 플러그인 4종이 함께 실립니다.** 이미지+텍스트 레이아웃 10종, 이모티콘/스티커(머블로봇 10종·Twemoji 24종), 문서 가져오기(TXT·MD·HTML·CSV), 문서 내보내기(Word). 켜고 끄는 목록은 에디터 `config.php` 의 `plugins` 이며, `full` 툴바를 쓰는 에디터에 자동으로 붙습니다. `minimal`·`compact` 나 항목을 직접 지정한 에디터는 그대로입니다. 스티커의 Twemoji 그래픽은 CC-BY 4.0 이므로, 공개 사이트에서 쓰면 출처 표기가 필요합니다(`plugins/stickers/twemoji/LICENSE.txt`)
- 에디터: 문서 내보내기의 PDF 는 페이지에 `html2pdf.js` 를 실은 경우에만 열립니다 — 라이브러리를 번들하지 않습니다
- 에디터: 체크리스트·목차 버튼이 `full` 툴바에 나옵니다. 코어 툴바 항목이지만 어느 프리셋에도 들어 있지 않아 슬래시 커맨드로만 닿을 수 있었습니다

### Fixed
- **쇼핑몰(관리자): 클레임 송장에 택배사를 고르지 않아도 저장되던 문제를 고쳤습니다.** 회수·재출고·고객 반송 송장이 송장번호만 필수여서, 택배사 없이 저장되면 추적 링크도 만들 수 없고 어디로 보냈는지 알 수 없는 송장이 남았습니다. 게다가 주문 화면의 송장 수정은 클레임 송장을 "교환 관리에서 처리해주세요"라며 거절하는데 정작 교환 관리에 수정 기능이 없어 **아무도 고칠 수 없었습니다.** 이제 택배사가 필수이고(폼·서버 양쪽), 반품·교환 관리의 배송 표에서 택배사·송장번호·메모를 고칠 수 있습니다
- 쇼핑몰(관리자): `교환 완료`·`반송 완료·종결` 버튼이 조건을 갖추기 전에는 눌리지 않고 이유를 알려 줍니다. 재출고(또는 반송) 송장이 배송완료여야 서버가 받아주는데 버튼은 열려 있어, 눌렀다가 거절당하고 나서야 알 수 있었습니다. 배송 표의 상태도 `READY`·`IN_TRANSIT` 대신 `준비`·`배송중`으로 나오고, 다음 단계 버튼도 `집화로`·`배송완료로`처럼 무엇이 되는지 보입니다
- **쇼핑몰: 교환 신청이 저장 단계에서 실패하던 문제를 고쳤습니다.** 회수지 우편번호 칸(`shop_returns.pickup_zipcode`)이 `VARCHAR(10)` 인데 거기 들어가는 값은 주문의 **암호화된** 우편번호(TEXT)였습니다. 고객이 회수지를 따로 적지 않으면 항상 그 값으로 채워지므로, 이 경로의 교환 신청은 `Data too long` 으로 통째로 실패했습니다. 컬럼을 TEXT 로 넓히고(마이그레이션 033), 고객이 직접 적은 우편번호도 다른 회수지 항목과 같이 암호화해 한 컬럼에 평문과 암호문이 섞이지 않게 했습니다. 관리자 화면에서 회수지 우편번호가 암호문으로 보이던 것도 함께 해결됩니다
- **쇼핑몰: 반품·교환 신청 창이 열려도 뒤 페이지가 같이 스크롤되던 문제를 고쳤습니다.** 후기·주문취소 창에는 있던 배경 스크롤 잠금과 최상위 배치가 이 창에만 빠져 있었습니다. 오버레이 클릭·ESC 로 닫는 동작도 함께 붙었습니다
- **쇼핑몰: 교환 신청 버튼을 누르면 후기 작성 창이 함께 뜨던 문제를 고쳤습니다.** 주문 상세의 후기 모달이 버튼의 표현용 클래스(`shop-order-view__review-btn`)로 열리고 있었는데, 교환 신청·신청 취소 버튼이 같은 줄에 맞추려고 그 클래스를 함께 쓰면서 후기 모달까지 열렸습니다. 이제 후기 버튼은 `data-review-detail` 로만 찾으므로 같은 줄에 버튼이 더 붙어도 끌려오지 않습니다
- **쇼핑몰: 배송을 끝내면 교환 신청이 열립니다.** 운송장은 등록해도, 배송완료로 찍어도 주문·주문상품 상태에 아무 영향을 주지 않는 별도 트랙이었습니다 — 이벤트는 발행되고 있었지만 받는 쪽이 없었습니다. 게다가 주문상품 상태는 주문 생성 때 `주문접수` 로 박힌 뒤 주문 상태를 따라오지 않아서, 주문이 배송완료가 되어도 주문상품은 계속 주문접수였습니다. 교환 신청 버튼은 주문상품 상태를 보므로 영영 나타나지 않았고, 관리자가 품목 상태를 결제완료→배송준비→배송중→배송완료로 네 번 직접 밟아야만 열렸습니다(품목 수만큼). 이제 운송장을 등록하면 그 운송장이 싣고 있는 상품이 `배송중` 으로, 배송완료로 찍으면 `배송완료` 로 함께 움직입니다. 여러 단계를 건너뛰어야 할 때 경유한 상태는 이력에만 남기고 고객 알림은 최종 상태로 한 번만 나갑니다 — 같은 배송 건으로 연락이 네 번 가지 않습니다. 배송 실패(FAILED)는 상태를 되돌리지 않습니다. 재배송인지 반송인지는 운영자가 판단할 문제이고, 자동으로 내리면 고객 화면이 널뜁니다
- **쇼핑몰: 주문 상태를 바꾸면 주문상품도 따라옵니다.** 주문 상태 변경은 주문상품을 건드리지 않아서, 운영자가 헤더만 올리는 평범한 운영에서 품목이 계속 뒤에 남았습니다. 이제 주문 상태를 바꾸면 그 주문의 상품도 함께 이동합니다. 클레임이 걸린 상품과 그 상태로 갈 수 없는 상품(이미 취소·반품된 상품 등)은 건드리지 않으며, 안내는 이미 나간 주문 상태 알림이 담당하므로 상품 단위 알림을 추가로 보내지 않습니다
- **쇼핑몰: 개별배송 주문의 상태가 실제 진행도를 따라갑니다.** 주문 상태 종합이 "전 상품이 같은 상태일 때"만 동작해서, 상품마다 배송이 갈리는 순간 — 정작 종합이 필요한 그 순간 — 멈췄습니다. 이제 가장 뒤처진 상품이 주문의 상태가 됩니다. 3개 중 1개만 배송완료면 주문은 배송중이고, 고객에게 "아직 다 오지 않았다"가 정확히 전달됩니다. 취소·반품이 끝난 상품은 종합에서 빼므로 1개 취소가 주문 전체를 취소로 되돌리지 않고, 전 상품이 같은 종료 상태로 모일 때만 주문도 그 상태가 됩니다. 종합 결과가 현재 상태보다 뒤이면 반영하지 않습니다 — 주문 상태가 뒤로 가는 일은 없습니다(품목이 헤더를 따라오지 못한 과거 주문에서 특히 잘 생기던 상황입니다)
- **쇼핑몰: 받은 상품을 먼저 구매확정할 수 있습니다.** 구매확정 판정이 주문 헤더 기준이라, 4개 중 1개만 배송완료인 주문에서는 아무것도 확정할 수 없었습니다(헤더는 가장 뒤처진 상품을 따라 결제완료입니다). 반대로 헤더만 배송완료로 올려둔 주문에서는 아직 오지도 않은 상품까지 한 번에 확정됐습니다. 이제 구매확정은 **상품 단위**입니다 — 배송이 시작된 상품에만 버튼이 뜨고, 그 상품만 확정됩니다. 전 상품이 확정되면 주문도 구매확정으로 올라가므로 **적립 등 확정 후처리는 종전대로 주문 단위로 한 번만 실행됩니다.** 판정은 교환과 같은 규칙(품목 우선·헤더 폴백)을 쓰며 `OrderService::canBuyerConfirmItem` 한 곳이 소유합니다. 반품·교환이 걸린 상품은 확정할 수 없습니다
- **쇼핑몰(관리자): 상품 상태를 한 번에 목표 단계까지 옮길 수 있습니다.** 상품별 `관리 ▸ 상태 변경`은 모든 상태를 선택지로 보여주면서 서버는 한 칸 전이만 허용해서, 결제완료 상품을 배송완료로 옮기려면 결제완료→배송준비→배송중→배송완료를 상품 수만큼 눌러야 했고 그때마다 "'결제완료'에서 '배송완료'(으)로 변경할 수 없습니다"가 떴습니다. 이제 상태 그래프에 전진 경로가 있으면 목표까지 걸어갑니다(선택 상품 일괄 변경과 같은 규칙). 경유한 상태는 이력에만 남고 알림은 최종 상태로 한 번 나갑니다. 뒤로 가는 변경은 종전대로 거부합니다
- **쇼핑몰: 교환 신청 버튼이 뜨는 규칙과 서버가 접수하는 규칙이 하나로 합쳐집니다.** 두 곳이 각자 판정하고 있어 갈릴 수 있는 구조였습니다. 이제 판정은 한 곳(`ExchangeService::isExchangeable`)이 소유합니다. 규칙은 주문상품이 배송완료면 허용, 아직 따라오지 못했으면 주문 상태가 배송완료인지로 판정입니다 — 부분 배송에서 먼저 도착한 상품만 교환할 수 있어야 하므로 품목 기준을 버릴 수 없고, 운송장을 쓰지 않는 상점에서는 주문 상태가 유일한 신호이기 때문입니다. 구매확정·취소·반품으로 넘어간 건은 어느 쪽이든 거부합니다
- **에디터: 에디터에서 만든 모습 그대로 저장됩니다.** 본문 정화기(rich 프로파일)의 인라인 스타일 허용 목록이 서식(색·글꼴·여백)까지여서, 에디터가 출력하는 테두리·배경·둥근 모서리·flex 가 저장 때 떨어져 나갔습니다. 그래서 인용구는 34종이 전부 같은 모양이 되고, 체크리스트는 체크박스가 통째로 사라지고, 이미지 레이아웃은 세로로 쌓였습니다. 정화기가 조용히 기능을 깨뜨리던 셈입니다. 이제 rich·block 이 같은 표현 CSS 를 받습니다. **넓힌 것은 CSS 뿐이며 스크립트 채널(script 태그·`on*` 핸들러·`javascript:`)은 그대로 막습니다.** `position`·`z-index` 는 회원 콘텐츠가 페이지 UI 위에 겹칠 수 있어(클릭재킹) 계속 막으므로, 이미지 오버레이 레이아웃과 영상 카드의 재생 배지는 겹치기 대신 세로로 놓입니다. 배경의 `url()` 은 관리자 조판(block)에만 열려 있고 게시판 본문에서는 색·그라디언트만 통과합니다. 체크박스는 `type` 을 필수로 두어 체크박스가 아닌 `input` 은 요소째 사라집니다 — 본문에 로그인 폼처럼 보이는 입력칸이 생기지 않습니다
- 에디터: `flex:0 0 40%` 처럼 퍼센트로 지정한 칸 비율에서 퍼센트만 사라져 레이아웃이 균등분할로 무너지던 문제를 고쳤습니다. 블록 HTML 에서도 같은 값이 이미 깨지고 있었습니다
- **쇼핑몰: 옵션 프리셋에 도메인 경계가 생깁니다.** 프리셋의 조회·수정·삭제·상품적용이 `preset_id` 만으로 이뤄져, 멀티도메인 환경에서 관리자가 자기 도메인 바깥의 프리셋을 읽고 고치고 지울 수 있었습니다. 상품은 이미 전 경로에서 도메인을 강제하고 있어 프리셋만 비어 있던 자리입니다. 프리셋을 상품에 적용할 때는 프리셋과 상품 **양쪽**의 도메인을 확인합니다 — 프리셋만 보면 남의 도메인 상품에 옵션을 심을 수 있습니다
- 쇼핑몰: 상품 목록의 `선택 삭제`에 확인 절차가 생깁니다. 관리자 목록 9개 중 상품만 확인 없이 즉시 지워졌습니다(단건 삭제에는 있었습니다)
- 쇼핑몰: 자동 생성되는 상품코드(`item_code`)가 충돌해 상품 등록이 실패하던 문제를 고쳤습니다. 코드는 도메인 무관 전역 유니크인데 4자리 난수로 뽑고 재시도가 없어서, 생일 문제로 하루 118건 정도면 충돌 확률이 50%에 이르렀습니다. 충돌하면 "상품 생성에 실패했습니다"라는 원인 불명 메시지만 남았습니다. 이제 그날의 마지막 번호 다음을 쓰고, 이미 있으면 다음 번호로 넘어갑니다. 기존 코드는 그대로 두며 형식(`G-YYYYMMDD-NNNN`)도 같습니다. 상품코드를 **직접 입력**해 겹친 경우도 같은 메시지만 나왔는데, 이제 저장 전에 걸러 "이미 사용 중인 상품코드입니다"라고 알려 줍니다 — 코드가 전역 유니크라 다른 도메인이 쓰는 코드와도 겹칠 수 있으나, 어느 도메인인지는 알려주지 않습니다
- **쇼핑몰: 할인·적립 유형이 계산에 반영됩니다. 저장된 금액이 달라질 수 있으니 릴리즈 후 상품 가격을 확인하세요.** 계산기가 유형(정률/정액)을 보지 않고 값이 100 미만이면 %, 이상이면 원으로 방식을 **추정**했습니다. 그래서 "정액 50원 할인"은 50% 할인으로, "정률 100% 할인"은 100원 할인으로 뒤집혔습니다. 이제 유형이 방식을 정합니다. 상품 폼의 기본 선택지인 "쇼핑몰 기본설정 적용"(`DEFAULT`)은 어디에서도 분기되지 않아 할인이 걸리지 않았는데, 이제 쇼핑몰 설정의 유형·값으로 해석됩니다. 설정이 다시 자기 자신을 가리키면(설정의 할인 유형을 `DEFAULT`/`기본 할인`으로 저장한 경우) 자기참조라 할인 없음으로 끊습니다 — 아래 Changed 의 선택지 정리를 함께 보세요. `기본 할인`(`BASIC`)으로 저장된 기존 설정은 옛 추정 규칙 그대로 계산하므로 끊기지 않습니다
- **쇼핑몰: 상품에 표시된 할인가가 장바구니·주문에도 적용됩니다. 실제 결제 금액이 내려갈 수 있습니다.** 장바구니·바로구매·체크아웃·주문 생성이 가격을 계산할 때 쇼핑몰 설정을 넘기지 않아, 설정 기반 할인("쇼핑몰 기본설정 적용"·"기본 할인")이 상품 화면에만 걸리고 담는 순간 정가로 돌아갔습니다. 표시가와 결제가가 갈라져 있던 셈입니다. 이제 네 경로 모두 같은 설정으로 계산합니다. 이미 담겨 있던 장바구니 항목은 체크아웃의 가격 변동 검증에 걸려 재확인을 요구하므로 금액이 조용히 바뀌지는 않습니다
- **쇼핑몰: 등급별 할인·적립이 실제로 적용됩니다.** 쇼핑몰 설정과 상품 등록 화면이 등급별 할인·적립을 저장해 왔지만 읽는 쪽이 없어서, "등급별 할인"을 고른 상품은 할인이 0 이었습니다. 이제 `discount_level_settings`·`reward_level_settings` 를 회원 등급에 맞춰 상품 목록·상세·기획전·상품 블록의 표시가와 장바구니·바로구매·체크아웃·주문의 결제가에 똑같이 적용합니다 — 한쪽만 반영하면 표시가와 결제가가 갈라지므로 함께 넣었습니다. 비회원이거나 해당 등급에 설정이 없으면 적용하지 않습니다. 설정은 `levelId` 를 키로 저장되고 회원은 `levelValue` 를 들고 있으므로, 그 사이를 잇는 조회를 `MemberLevelResolver` 한 곳에 모아 요청 단위로 캐시합니다(포인트 사용 한도를 `point_level_settings` 에서 읽는 방식과 같습니다)
- 쇼핑몰: 상품에 `할인 값`을 `0` 으로 두고 `기본 할인`을 고르면 쇼핑몰 설정값을 읽기도 전에 "할인 없음"으로 빠져나가던 문제를 고쳤습니다. 값이 상품 자신에게서 오는 유형(정률·정액)에만 0 검사를 적용합니다
- 설치: 공유호스팅에서 DB 연결 테스트가 접속에 성공하고도 실패로 표시되던 문제를 수정했습니다. 연결 테스트는 서버에만 접속한 뒤 `SHOW DATABASES` 로 데이터베이스 존재를 확인했는데, 이 문장은 글로벌 권한을 요구해서 `skip-show-database` 로 운영하는 호스팅에서는 계정이 정상이어도 거절당했습니다. 이제 실제 설치 단계와 똑같이 `dbname` 을 포함해 접속해 보는 것으로 존재와 접근 가능 여부를 함께 확인하므로, 목록 조회 권한이 필요 없습니다. 연결 테스트를 통과했는데 설치가 실패하는 경우도 사라집니다 — 테스트가 검증하는 연결과 설치가 사용하는 연결이 같아졌습니다. "없으면 자동 생성" 은 그대로이며, 데이터베이스가 없다는 응답을 받았을 때만 폴백으로 시도합니다
- 설치: DB 연결 실패 원인이 구분되어 표시됩니다. 이전에는 비밀번호 오류·데이터베이스 이름 오류·서버 접속 불가가 모두 `DB 연결 실패: <드라이버 원문>` 한 줄로 합쳐져 무엇을 고쳐야 하는지 알 수 없었습니다. 특히 권한이 제한된 계정에서는 서버가 "없는 데이터베이스"와 "권한 없는 데이터베이스"를 구분해 주지 않으므로(둘 다 `1044`), 해당 메시지는 이름 확인과 호스팅 관리 페이지에서의 생성 두 가지를 함께 안내합니다
- 설치: 데이터베이스 존재 확인에 쓰던 `SHOW DATABASES LIKE` 의 `_` 가 LIKE 와일드카드로 동작해, `myuser_db` 같은 이름이 다른 데이터베이스에 잘못 일치할 수 있던 문제가 함께 해소되었습니다. 호스팅이 발급하는 이름은 대부분 밑줄을 포함합니다
- 설치: 데이터베이스 설정 화면이 브라우저 자동완성과 암호관리자에 반응하지 않습니다. 이 화면이 받는 값은 사람의 로그인 자격증명이 아니라 서버 설정값인데, "아이디+비밀번호" 모양 때문에 로그인 폼으로 인식되어 사이트 계정이 채워지거나 설치 후 비밀번호 저장을 묻는 일이 있었습니다. 그렇게 저장된 항목은 이 사이트의 로그인 정보로 남아 이후 엉뚱한 화면에 채워집니다. 관리자 계정을 만드는 단계는 저장되는 편이 이득이므로 그대로 둡니다

### Changed
- **쇼핑몰(관리자): 클레임 상태 알림 설정이 `상태·알림 설정` 화면으로 옮겨집니다.** 주문 상태 액션은 설정 화면에, 클레임 상태 액션은 목록 화면 상단에 있어 같은 성격의 설정이 두 곳에 흩어져 있었습니다(클레임이 나중에 들어오며 자기 설정 화면이 없어 목록에 얹혔던 자리입니다). 이제 둘 다 `상태·알림 설정`(옛 `주문상태 설정`)에 모입니다. 클레임 쪽에는 **상태 흐름은 편집 대상이 아니라는 안내**가 붙습니다 — 회수·검수·재출고/환불은 실물 절차에 묶여 있어 제품이 소유합니다. `반품·교환 관리`는 목록과 처리만 남은 운영 화면이 되고, 설정으로 가는 링크가 우상단에 있습니다
- **쇼핑몰(관리자): 주문 목록에서 진행 중인 반품·교환이 보입니다.** 상품 옆에 `반품·교환 진행중` 배지가 붙고 누르면 그 주문의 클레임으로 갑니다. 이전에는 주문 상세로 들어가 봐야만 알 수 있어 처리 대기 중인 건을 놓치기 쉬웠습니다. 완료·거절·취소된 건은 표시하지 않습니다. 클레임 상세에는 `주문 상세`로 돌아가는 버튼이 생겼습니다 — 주문에서 들어온 경우 `목록` 버튼만 있어 왔던 곳으로 돌아갈 수 없었습니다
- 쇼핑몰(관리자): 화면 안에서 영역을 넘나드는 링크에 `activeCode`가 붙어 사이드바 하이라이트가 따라옵니다. 주문에서 클레임으로 넘어가도 메뉴는 계속 주문 관리를 가리키고 있었습니다
- 쇼핑몰(관리자): 표기를 `반품·교환` 순서와 미드닷으로 통일했습니다(`교환·반품`·`교환/반품`·`반품/교환`이 섞여 있었습니다). 같은 화면의 `결제·환불 내역`도 함께 맞췄습니다. 문장 속 열거(`생성/수정`, `회원/비회원` 등)는 "또는"으로 읽히는 자리라 슬래시를 유지합니다
- **쇼핑몰(관리자): `교환 관리`가 `반품·교환 관리`가 되고 주소가 `/admin/shop/claims`로 바뀝니다.** 두 유형이 같은 워크플로우를 쓰므로 화면도 하나입니다. 목록에 유형 필터와 유형 열이 생기고, 상태 이름은 유형에 맞춰 나옵니다(같은 `COMPLETED`가 교환에서는 교환완료, 반품에서는 반품완료). 반품 상세에는 교환 대상 옵션 대신 환불 예정액과 차감될 반품비가 표시됩니다. **주문 상세의 상품별 `관리`에서 반품 승인·거절이 빠집니다** — 이제 반품·교환 관리에서 회수·검수를 거쳐 처리합니다. 반품 접수는 주문 상세에 그대로 남으며(전화 요청을 관리자가 대신 접수), 수량을 지정할 수 있습니다. 기존 반품 데이터는 상태값이 그대로 호환되며, 구 흐름으로 이미 완료된 건에는 회수·검수 기록이 없다는 메모가 붙습니다(마이그레이션 032)
- 쇼핑몰(확장 개발): `ExchangeService`가 두 유형을 모두 소유하게 되어 `ClaimService`로 이름이 바뀌었습니다. `isExchangeable()`은 `isClaimable()`, `ClaimRepository::hasBlockingNonExchangeClaim()`은 `hasBlockingClaimOfOtherType()`, `hasCompletedExchange()`는 `hasCompletedClaim()`입니다. `request()`에 클레임 유형 인자가 붙었고(기본값 `EXCHANGE`), 반품 종료용 `completeRefund()`가 생겼습니다. 검수 결과(`inspection_result`)와 회수품 재고 복구 시각은 교환 대상 상품 테이블에서 클레임 본체(`shop_returns`)로 옮겼습니다 — 반품에는 교환 대상 행이 없어 적을 곳이 없었습니다. `OrderService::requestItemReturn()`·`processItemReturn()`은 제거했습니다(클레임 워크플로우가 대신합니다)
- 쇼핑몰(스킨): 주문 상세 스킨의 `$exchangeClaimsByDetail`이 `$claimsByDetail`로 바뀌고(반품·교환을 모두 담습니다) `$claimableByDetail`이 추가됩니다. 고객 클레임 신청 경로도 `POST /shop/order/{orderNo}/items/{detailId}/exchange` → `.../claim`(`return_type` 포함)으로 바뀌었습니다
- **쇼핑몰(관리자): 주문 상세 레이아웃이 처리 흐름을 따라 재배치됩니다.** 상단에 `주문 정보`·`배송 정보`·`결제 정보`가 3등분으로 놓이고, 그 아래 `주문 상태` 카드(주문 전체에 적용되는 자리라 배경을 살짝 달리했습니다), 이어서 주문 상품과 배송 운송장이 전폭으로 옵니다. 나머지(반품·교환, 결제·환불, 추가 정보, 이력 / 환불 처리, 메모)는 8:4로 남습니다. 주문 상태 카드는 상품 목록 바로 위에 두었습니다 — 이번에 주문 상태 변경이 전 품목 벌크가 되었으므로 그것이 무엇에 작용하는지 옆에서 보여야 하고, 카드에는 "이 주문의 상품이 모두 함께 옮겨집니다. 일부만 옮기려면 아래에서 선택해 적용하세요"라는 안내가 붙습니다
- **쇼핑몰(관리자): 주문 상품에서 여러 상품을 골라 배송 단계를 한 번에 바꿀 수 있습니다.** 각 행과 묶음 머리, 표 머리에 선택 상자가 생기고(묶음 상자는 그 묶음만 집습니다), 표 아래에서 `배송준비`·`배송중`·`배송완료` 중 하나를 골라 적용합니다. 여러 단계를 건너뛰어야 하면 서버가 상태 그래프를 따라 걸어가므로 한 번에 도착하며, 옮길 수 없는 상품(이미 지난 단계, 반품·교환 진행 중)은 건너뛰고 몇 개가 바뀌고 몇 개가 남았는지 알려 줍니다. 반품·교환이 걸린 상품은 선택 상자 자체가 잠깁니다. **취소·반품·구매확정은 목록에 없습니다** — 환불·재고·적립 후처리가 딸려 있어 전용 흐름이 담당하며, 여기서 상태만 바꾸면 그 후처리를 통째로 건너뛰기 때문입니다
- **쇼핑몰(관리자): 주문 상세가 출고 단위를 중심으로 재배치됩니다.** 무엇을 어떻게 보내야 하는지는 주문 상품 목록에서 읽혀야 하는데, 배송 관련 카드가 다섯 칸 아래에 따로 있어 상품과 배송을 눈으로 짜맞춰야 했습니다. 이제 배송비 묶음이 둘 이상인 주문은 주문 상품 표가 묶음별로 나뉘어 나오고, 묶음 머리에 그 묶음의 송장(택배사·송장번호·배송상태)이나 `송장 미등록` 표시와 `송장 등록` 버튼이 함께 붙습니다. 버튼을 누르면 아래 운송장 폼이 그 묶음으로 맞춰지고 커서가 송장번호 칸으로 갑니다. 배송지·운송장 카드도 주문 상품 바로 아래로 올라옵니다. 묶음이 하나뿐인 주문(대부분)은 종전과 같은 평면 목록입니다
- 쇼핑몰: 주문 상세에서 아직 쓸 수 없는 후기의 자리를 잡아두던 비활성 `후기 작성` 버튼이 사라집니다. 같은 줄에 교환 신청이 들어오면서, 지금 할 수 있는 일과 나중에 할 수 있는 일이 뒤섞여 읽혔습니다. 후기를 쓸 수 있게 되면 그때 버튼이 나옵니다 (판정값 `$meta['pending']` 은 스킨이 쓸 수 있도록 그대로 전달합니다)
- 쇼핑몰(스킨): 주문 상세 스킨에 `$shipmentItemNames`(운송장 id → 상품명 목록)가 전달됩니다. 배송 묶음이 둘 이상인 주문에서만 채워지며, 기존 커스텀 스킨은 이 값을 쓰지 않아도 그대로 동작합니다
- 쇼핑몰(확장 개발): `OrderService` 에 `advanceItemsToState()`·`advanceItemsToAction()`·`findStateIdByAction()` 이, `ExchangeService` 에 `isExchangeable()` 이, `ShipmentService` 에 `getShippingGroups()`·`itemNamesByShipment()` 가 생겼습니다. `ShipmentService` 생성자의 네 번째 인자(`ShipmentGroupResolver`)는 선택이므로 기존 호출부는 그대로 둡니다
- **에디터: 툴바의 인용구 버튼이 스타일 갤러리를 엽니다.** 이전에는 누르는 즉시 기본 `blockquote` 로 감쌌습니다. 선택한 텍스트가 있으면 고른 스타일로 감싸고, 없으면 예시 문장과 함께 넣습니다. 스킨이 `toolbarItems` 에 `blockquote` 를 넣어 쓰고 있다면 버튼 위치는 그대로지만 눌렀을 때의 동작이 달라집니다
- 에디터: 실행취소·다시실행이 브라우저의 `execCommand` 대신 에디터 자체 기록을 씁니다. 브라우저마다 다르던 되돌리기 단위가 같아지고 커서 위치까지 복원됩니다
- 쇼핑몰: 설정 화면의 할인·적립 유형 선택지에서 `쇼핑몰 기본설정 적용`·`기본 할인`이 빠집니다. 쇼핑몰 설정 자신을 가리키는 자기참조라 어떤 할인으로도 풀리지 않는 값이었습니다. 이제 없음·등급별·정률·정액만 고를 수 있습니다. 상품 등록 화면은 그대로입니다 — 상품에서 "쇼핑몰 기본설정 적용"은 유효한 선택입니다

### Removed
- 에디터: 배포본에서 개발용 파일을 걷어냈습니다 — `demo.html`, `plugins/upload/upload.html`, `README.md`, `package.json`, `MubloEditor.d.ts`. 앞의 둘은 `public` 아래라 주소만 알면 그대로 열리던 페이지였고, 나머지는 이 저장소에서 참조하는 곳이 없습니다
- 쇼핑몰: 대응 메서드가 없어 호출하면 500 이 나던 상품 이미지·상세 삭제 라우트(`/admin/shop/products/delete-image`·`delete-detail`)를 제거했습니다. 컨트롤러에 해당 메서드가 없고 호출하는 화면도 없어, 동작하던 기능이 빠지는 것은 아닙니다
- **쇼핑몰: `쇼핑몰 → 등급별 혜택` 관리자 화면이 사라집니다.** 이 화면이 저장하던 등급 가격정책(할인율·적립율·무료배송·자동쿠폰)은 어떤 계산에도 연결된 적이 없어, 값을 넣어도 아무 일도 일어나지 않았습니다. 등급별 할인·적립의 저장소는 위 Fixed 의 JSON 설정(쇼핑몰 설정 화면의 "레벨별 할인 값"과 상품별 등급 설정)으로 확정했으므로, 같은 목적의 화면 두 벌 중 쓰이지 않던 쪽을 걷었습니다. **무료배송·자동쿠폰은 이 화면에서도 동작한 적이 없으며, 이번 변경으로 새로 빠지는 기능은 없습니다.** `shop_level_pricing` 테이블은 남겨 둡니다 — 삭제는 되돌릴 수 없으므로 정리 시점은 따로 판단합니다

## [1.2.0] - 2026-08-12

### Added
- 에디터: `data-toolbar-mobile`·`data-toolbar-items-mobile`·`data-toolbar-breakpoint`로 모바일 툴바를 따로 구성할 수 있습니다. Board 기본·갤러리 글쓰기는 768px 이하에서 필수 편집 버튼만 표시합니다
- 에디터: 툴바 프리셋에 `compact`(7개 — 되돌리기·되돌리기 취소·굵게·기울임·밑줄·링크·이미지)를 추가했습니다. 기존 `minimal`(3개)과 `basic`(20개) 사이가 비어 있어 좁은 화면이나 좁은 칸에 쓸 프리셋이 없었습니다. 320px 폭에서 한 줄에 들어가며, `toolbar` 와 `toolbarMobile` 양쪽에서 쓸 수 있으므로 스킨이 버튼 이름을 나열하지 않아도 됩니다
- Core: `MemberUpdatedEvent` 계열에 `getPreviousLevelValue()` 를 추가했습니다. 수정 **전** 등급값을 스칼라로 돌려주므로, 옛 등급을 알기 위해 회원 엔티티를 관통할 필요가 없습니다. 변경 **후** 등급은 이 이벤트가 들고 있지 않습니다 — 커밋된 값이므로 `MemberQueryInterface` 로 조회하세요
- Core: `DomainDeletedEvent::getDomainName()`·`DomainUpdatedEvent::getPreviousValues()`·`BlockPageRenderingEvent` 의 페이지 접근자(`getPageId()`·`getDomainId()`·`getPageCode()`·`getPageTitle()`·`getPageConfig()`)를 추가했습니다. 아래에서 제거한 엔티티 게터의 자리를 대신합니다 — 삭제된 도메인 이름과 수정 전 도메인 값은 이벤트가 지나가면 다시 조회할 수 없으므로 그대로 실어 보냅니다
- 개발 도구: `phpunit.xml.dist` 가 패키지 테스트도 플러그인처럼 와일드카드로 수집합니다. 패키지를 추가할 때 이 파일을 고칠 필요가 없고, 중첩 Plugin(`packages/*/Plugins/*/tests`)의 테스트도 함께 수집됩니다 — 이전에는 어느 경로에도 걸리지 않아 작성해도 실행되지 않았습니다. 새 패키지는 `composer.json` 의 autoload-dev 에 `Tests\<이름>\` 만 등록하면 됩니다
- 관리자: 확장 업로드 모달이 열릴 때 `plugins/`·`packages/` 에 실제로 쓸 수 있는지 미리 확인합니다. 쓸 수 없으면 파일 선택과 업로드 버튼이 비활성화되고 원인과 해법(그룹 쓰기 권한 또는 FTP 직접 배치)을 그 자리에서 안내하므로, zip 을 고르고 업로드를 누른 뒤에야 실패를 알게 되던 흐름이 사라집니다. 확인은 퍼미션 숫자가 아니라 실제 파일 생성·삭제라 ACL·open_basedir 까지 반영되며, 비용이 있으므로 모달을 열 때 한 번만 수행합니다. 안내는 `707` 대신 그룹 쓰기(`775`)나 FTP 직접 배치를 권합니다 — 코드가 실행되는 디렉토리를 웹 유저에게 상시 열어 두는 편이 손해가 큽니다
- 개발 도구: 이벤트 payload 누출을 양쪽에서 검사합니다. 이벤트는 정책상 안정 API 라 기존 확장 API 검사가 무조건 통과시켰고, 그 탓에 이벤트가 반환하는 내부 타입을 타고 들어가는 의존(`$event->getMember()->getLevelValue()`)은 `use` 문이 없어 검사에 보이지 않았습니다. 생산자 쪽은 새 검사(`composer check-event-payload`)가 이벤트 정의를 보고, 소비자 쪽은 `check-extension-api` 가 이벤트 클래스를 찾아가 게터의 반환 타입을 읽어 판정합니다. 확장이 자기 소유 타입을 자기 이벤트로 주고받는 것은 내부 응집이므로 양쪽 모두 통과시킵니다. 두 검사 모두 동결분 0건에서 시작합니다

### Changed
- **VisitorStats**: 전환 통계가 폼 확장의 테이블(`form_submissions`·`forms`)을 직접 조회하지 않고, `ConversionRecordedEvent` 로 통보된 전환만 집계합니다. 전환의 갈래는 계약이 실어 온 `sourceType`·`sourceLabel` 로 구분하므로 주문·상담·가입·폼 접수가 한 화면에서 같은 축으로 보입니다. 이에 따라 "폼별 전환"은 "소스별 전환"이 되고, 전환 목록의 필터도 폼 선택에서 소스 선택으로 바뀌며, 목록에서 IP 열이 빠집니다(이벤트 계약에 없는 값입니다). 관리자 API 응답도 함께 바뀌었습니다 — `/api/conversion-stats` 는 `byForm`·`topForm` 대신 `bySource`·`topSource` 를, `submissions` 대신 `recorded` 를 돌려주고, `/api/conversions` 는 요청 필드가 `form_id` → `source_type` 이며 항목이 `submission_id`·`created_at`·`form_name`·`ip_address` 대신 `conversion_id`·`occurred_at`·`source_type`·`source_label`·`status` 를 담습니다
- 관리자: 확장 업로드 성공 메시지가 토스트에서 확인 모달로 바뀌었습니다. 설치 결과에는 서명 확인 여부와 활성화 방법이 담기는데, 기존에는 0.8초 뒤 새로고침이 메시지를 잘라 읽을 수 없었습니다. 이제 확인을 눌러야 목록을 새로고침합니다
- 관리자: 확장 업로드가 퍼미션으로 실패했을 때의 메시지가 `777` 을 연상시키는 "서버 퍼미션을 확인하세요" 대신 그룹 쓰기 권한과 FTP 직접 배치를 안내합니다
- Core: 전환 소스 타입(`ConversionSourceTypes`)은 **코어 자신의 개념만** 담습니다. 확장이 발행하는 타입은 그 확장이 소유하며 코어에 등재하지 않습니다. 등재는 강제가 아니어서 어떤 문자열이든 그대로 집계되고, 표시 이름은 발행 쪽이 `ConversionRecordedEvent::$sourceLabel` 로 실어 보내는 값이 우선합니다 — 한 타입 안의 갈래(폼 제목·상품군 등)는 이 값으로 구분하며, 통계 화면도 타입+라벨 단위로 나눠 집계합니다

### Removed
- Core: 이벤트가 코어 내부 엔티티를 그대로 돌려주던 게터 10개를 제거했습니다 — `MemberRegisteredEvent`·`MemberUpdatedEvent`·`MemberDeletingEvent`·`MemberDeletedEvent`·`MemberWithdrawingEvent`·`MemberWithdrawnEvent`·`MemberLoggedInEvent` 의 `getMember()`, `DomainDeletedEvent::getDeletedDomain()`, `DomainUpdatedEvent::getPreviousDomain()`, `BlockPageRenderingEvent::getPage()`. 이벤트는 안정 API 라서, 엔티티를 통째로 넘기면 코어 내부 표면이 그대로 확장에 중계되고 코어를 리팩터링할 때마다 확장이 깨집니다. 번들 확장·코어 어디에도 사용처가 없었고 payload 세부가 문서화된 적도 없습니다. **회원 정보는 `MemberQueryInterface`** 로 조회하세요 — 이벤트가 들고 있던 엔티티는 어차피 수정 전 스냅샷이라 변경 후 값을 읽는 용도로는 처음부터 맞지 않았습니다. 도메인·블록 페이지 값은 위에 추가된 스칼라 접근자를 쓰세요
- **VisitorStats**: 폼 확장의 테이블을 직접 읽던 `ConversionRepository` 를 제거했습니다. 이 저장소는 `form_submissions` 존재 여부만 확인하고 `forms` 는 확인하지 않은 채 조인했으며, `outcome = 'success'` 같은 컬럼·값 계약까지 남의 스키마에 의존하고 있었습니다. 해당 확장이 없는 설치본에서는 전환 화면이 언제나 0 이었고, 미설치와 실적 0 건이 화면에서 구분되지도 않았습니다
- Core: `ConversionSourceTypes` 에서 특정 확장의 소스 타입 상수 `RENTAL_ORDER`·`RENTAL_CONSULTATION` 를 제거했습니다. 코어가 확장 목록을 들고 있으면 그 확장이 없는 설치본에서 죽은 이름이 되고, 확장이 이름을 바꿔도 코어는 알 수 없습니다. **저장된 데이터와 집계는 영향을 받지 않습니다** — 와이어 값(`rental_order` 등)은 그대로이고, 표시 이름은 기록 시점에 이미 행에 저장되어 있습니다. 이 상수를 참조하던 확장은 자기 상수를 정의하거나 문자열 리터럴로 바꾸고, 표시 이름은 `ConversionRecordedEvent::$sourceLabel` 로 실어 보내세요
- **VisitorStats**: 관리자 API `/api/form-conversions` 와 `/api/event-conversions` 를 제거했습니다. 앞의 것은 어떤 화면도 호출하지 않는 경로였고, 뒤의 것은 전환 통계가 하나로 합쳐지면서 `/api/conversion-stats` 응답의 `bySource` 로 흡수되었습니다
- Core: 알림 템플릿 헬퍼(`NotificationTemplateUiHelper`)에서 폼 확장의 테이블(`forms`·`form_fields`)을 직접 조회하던 `loadActiveFormsWithMeta()`·`buildExampleBodies()`·`buildSamplePreviewValues()` 를 제거했습니다. 코어가 특정 확장의 스키마를 알고 있던 자리이며, 안정 계약(`NotificationTemplateContextInterface`)에 노출된 적이 없고 호출하는 곳도 없었습니다. 확장이 제공하는 알림 변수는 이전부터 `CollectNotificationVariablesEvent` 를 구독해 각 확장이 채워 넣는 경로로 흐르고 있으므로 동작은 달라지지 않습니다. 이 구체 클래스를 직접 참조해 세 메서드를 부르던 코드가 있다면 해당 이벤트 구독으로 옮기세요
- Core: 보안 파일 다운로드 권한 검증에서 `autoform` 파일 카테고리 하드코딩을 제거했습니다. 코어가 확장의 카테고리 이름을 알던 분기로, 확장용 위임 경로(`SecureFileAccessEvent`)가 이미 있는데도 그 앞을 가로채고 있었습니다. 이벤트를 거쳐도 grant 하는 구독자가 없으면 안전 기본값인 관리자 전용으로 판정하므로 접근 허용 범위는 그대로이며, 거부 시 남는 경고 로그 문구만 달라집니다. 이 카테고리를 관리자 외에게 열려면 `SecureFileAccessEvent` 를 구독해 grant 하세요

### Fixed
- 관리자: 확장 업로드가 `plugins/`·`packages/` 디렉토리 **부재**까지 "쓰기 권한이 없습니다" 로 표시하던 문제를 수정했습니다. 두 원인이 한 메시지로 묶여 있어, 설치본이 온전하지 않은 경우에도 엉뚱하게 퍼미션을 조정하게 됐습니다
- **Shop**: 등급 변경 쿠폰(`LEVEL` 트리거)이 변경 **전** 등급으로 발행 대상을 판정하던 문제를 수정했습니다. 허용 등급을 지정한 정책에서 승급한 회원은 쿠폰을 받지 못하고, 강등된 회원이 오히려 상위 등급 쿠폰을 받았습니다. 이제 커밋된 등급을 `MemberQueryInterface` 로 재조회해 판정합니다. 허용 등급을 비워 둔 정책은 이전에도 등급을 보지 않았으므로 영향이 없습니다

## [1.1.0] - 2026-08-07

### Added
- Core: 회원 공개 식별자 `public_id` 를 도입했습니다. 22자 소문자 16진수이며, 타인을 클라이언트에서 가리키는 HTML·URL·JSON 은 이제 내부 `member_id` 나 로그인 아이디 대신 이 값을 씁니다. 기존 회원은 마이그레이션이 값을 채워 넣습니다
- Core: 남에게 보이는 이름을 만드는 `publicDisplayName()` 을 `AuthenticatedUser` 와 `MemberProfile` 에 추가했습니다. 닉네임이 없으면 `회원 {public_id 앞 12자}` 로 떨어지며, 실명이나 로그인 아이디로는 떨어지지 않습니다
- Core: 확장이 회원 문맥 액션(쪽지 보내기·프로필 보기 같은 작성자 메뉴 항목)을 등록할 수 있는 Contract 를 추가했습니다 — `MemberActionQueryInterface`, `MemberActionDefinition`, `MemberActionView`, `MemberActionScope`, `MemberActionVariant`, `MemberActionTargetTransport`, `MemberActionStateResolverInterface`, `MemberActionStateScope`, `MemberExtensionIdentity`, 그리고 `MemberActionBuildingEvent`. 소비자는 구현 클래스나 URL 을 알지 않으며, 액션을 등록한 확장을 비활성화하면 메뉴에서 사라집니다
- Core: 작성자 메뉴를 렌더링하는 뷰 헬퍼 `memberActionMenu($actions, $targetPublicId, $options)` 와 컴포넌트 자산을 추가했습니다
- Core: 원장·잔액 기반 랭킹 조회 계약 `BalanceRankingQueryInterface` 와 부속 타입(`BalanceRankingEntry`, `BalanceRankingFilter`, `BalanceRankingMetric`, `BalanceRankingPage`)을 추가했습니다
- 관리자: 시스템 관리 화면에 회원 액션 정의 진단 패널이 추가되어, 확장이 등록한 액션의 문제를 도메인별로 확인할 수 있습니다

### Changed
- **Board 스킨**: 글·댓글 데이터에서 `member_id` 가 빠지고 `author_public_id` 와 `author_actions` 가 제공됩니다. 작성자 동작은 `$this->memberActionMenu($article['author_actions'], $article['author_public_id'], [...])` 조합으로 렌더링해야 합니다. 내부 회원 번호나 로그인 아이디로 프로필·쪽지 URL 을 직접 조립하던 커스텀 스킨은 수정이 필요합니다. 번들 스킨(basic·gallery)은 이미 반영되어 있으며 자세한 내용은 `SKIN-GUIDE.md` 를 참고하세요
- Core: `MemberQueryInterface` 에 `findByPublicId()`, `searchActiveByNickname()`, `publicIdsFor()` 가 추가되었습니다. 이 인터페이스를 직접 구현한 확장은 세 메서드를 함께 구현해야 합니다
- Core: 회원 검색이 닉네임만 대상으로 합니다. 로그인 아이디로는 다른 회원을 찾을 수 없습니다
- Shop: 리뷰·문의 작성자 표시가 닉네임이 없을 때 로그인 아이디로 떨어지던 동작을 없애고 공개 표시명을 사용합니다
- Qna: 문의 작성자·운영자 표시에 공개 표시명을 사용합니다
- Board·Shop·Qna: 기존 글·댓글·리뷰·문의에 저장된 작성자 표시 데이터를 마이그레이션이 공개 표시명으로 정리합니다. 로그인 아이디가 노출되어 있던 과거 데이터가 함께 정정됩니다
- 프론트: 마이페이지 기본 레이아웃이 사용자 정보를 `AuthenticatedUser` 에서 직접 읽도록 정리되었습니다
- 관리자: 블록 에디터의 스크립트를 뷰 인라인에서 정적 자산(`assets/js/admin/blockeditor.js`)으로 분리했습니다. 브라우저가 캐시하므로 편집 화면 재방문이 빨라집니다. 이 뷰를 직접 수정해 쓰던 경우 스크립트 위치가 바뀐 점에 유의하세요
- 관리자: 포인트 내역 화면의 총 건수 조회가 빨라졌습니다. 원장에 `(domain_id, created_at, member_id, amount)` 인덱스를 추가해 인덱스만으로 집계합니다 (100만 행 기준 110.6ms → 19.5ms)
- Board: 게시판 목록과 총 건수 조회가 빨라졌습니다. `(domain_id, board_id, status, is_notice, created_at)` 커버링 인덱스를 추가하고, 공지 여부가 이미 필터로 고정된 목록에서 중복 정렬을 제거했습니다 (20만 행 기준 목록 8.9ms → 3.5ms, 건수 14.9ms → 3.3ms)
- 업그레이드: 이 릴리즈는 마이그레이션을 포함합니다 — Core `026`(회원 `public_id`)·`027`(원장 인덱스)·`028`(메뉴 제공자명 정정), Board `004`(작성자 표시명 백필)·`005`(목록 인덱스), Shop `030`, Qna `003`. **기존 설치본은 파일을 교체해도 마이그레이션이 자동으로 적용되지 않습니다.** 업데이트 후 관리자 → 시스템 관리에서 대기 중인 마이그레이션을 실행하세요
- 업그레이드: Core `026` 이 회원 `public_id` 를 채운 뒤에 Board·Shop·Qna 의 작성자 표시명 백필이 의미를 갖습니다. 시스템 관리 화면의 실행 순서를 그대로 따르면 됩니다

### Deprecated
- Core: `MemberIdentity::getUserId()` — 다음 major 에서 제거합니다. 신규 코드는 `getPublicId()` 와 `getDisplayName()` 을 사용하세요. 기존 안정 이벤트의 하위 호환을 위해 당분간 유지됩니다

### Fixed
- 메뉴: 확장이 만든 메뉴 항목의 제공자명이 소문자로 저장되던 문제를 수정했습니다. 그 탓에 메뉴 관리에서 항목을 수정할 때 제공자가 선택되지 않았고, 마이페이지 사이드바가 확장 아이콘 대신 기본 아이콘을 표시했습니다. 데이터베이스 조회는 대소문자를 구분하지 않아 다른 기능은 정상 동작했기에 드러나지 않던 문제입니다. 마이그레이션이 기존 데이터도 함께 정정합니다

## [1.0.1] - 2026-08-05

### Fixed
- 메뉴: 같은 메뉴를 자기 하위에 넣거나 최대 깊이(10단계)를 넘기는 트리 저장을 거부합니다. 이전에는 그대로 저장되어 경로가 어긋났습니다
- 메뉴: 메뉴명을 변경할 때 하위 경로의 이름이 일부만 갱신되던 문제를 수정했습니다. 경로명을 경로 코드에서 다시 만들므로 이미 어긋나 있던 기존 데이터도 함께 정리됩니다
- 메뉴: 메뉴명 변경을 하나의 트랜잭션으로 묶어, 중간에 실패해도 브레드크럼에 옛 이름이 남지 않습니다
- Board: 글 개별 권한 레벨이 명시적으로 지정되지 않으면 게시판 설정을 상속합니다. 이전에는 빈 값이나 숫자가 아닌 값이 `0`(비회원 허용)으로 저장되어, 회원 전용으로 설정한 게시판인데도 그 글만 비회원에게 열리는 일이 생길 수 있었습니다. 판단할 수 없는 입력은 이제 상속으로 되돌아갑니다
- Board: 글 개별 레벨로 게시판 설정보다 권한을 낮추는 것은 관리자만 가능합니다. 조이는 방향은 그대로 누구나 할 수 있습니다. 스킨이 작성 폼에서 레벨을 입력받더라도 일반 회원이 게시판 정책을 우회할 수 없습니다
- Board: 관리자 화면이 게시판 설정과 글 개별 설정의 차이를 드러냅니다. 글 폼의 "게시판 설정 사용" 옵션에 게시판의 현재값을 함께 표시하고, 개별 설정이 게시판보다 낮은 글은 목록과 폼에서 경고로 표시합니다. 이전에는 "게시판 설정 사용"과 "Lv.0 (전체)"이 화면에서 구분되지 않았습니다
- Board: 다운로드 권한이 없는 첨부도 클릭을 받고, 이유를 모달로 알려줍니다. 비회원에게는 "로그인 후 다운로드 가능합니다"와 함께 로그인 버튼을(로그인 후 원래 글로 복귀), 레벨이 모자란 회원에게는 권한 없음을 알립니다. 이전에는 회색 글씨로 반응 없이 남아 있어 링크가 고장 난 것인지 권한이 모자란 것인지 알 수 없었습니다 (basic·gallery 스킨)
- Board: 다운로드 주소로 직접 접근했을 때 브라우저에 JSON 원문이 그대로 보이던 문제를 수정했습니다. 비회원은 로그인 페이지로, 권한이 없는 회원은 403 페이지로, 없는 첨부는 404 페이지로 보냅니다. 스크립트 요청(XHR·`Accept: application/json`)은 이전처럼 JSON 을 받되 상태 코드가 401/403/404 로 구분됩니다
- Board: 첨부 다운로드 주소를 스킨이 조립하지 않고 `download_url` 을 그대로 출력하도록 바꿨습니다. 식별자 규칙이 바뀔 때마다(예: 마이그레이션 003 의 `attachment_id` → `public_id`) 갱신되지 않은 스킨이 조용히 404 가 되던 문제를 끊습니다. 커스텀 스킨은 `SKIN-GUIDE.md` §2-3 을 따라 `download_url` 로 교체해 주세요. 주소를 만들 수 없는 첨부는 깨진 링크 대신 "받을 수 없는 첨부입니다" 로 표시됩니다
- Board: 게시글 보기에서 첨부파일 다운로드 링크가 동작하지 않던 문제를 수정했습니다. 스킨에 전달되는 첨부 데이터에서 `public_id` 가 누락되어 링크 주소가 잘린 채 만들어졌고, 그 결과 다운로드 횟수도 오르지 않고 "다운로드 N" 표시도 나타나지 않았습니다 (basic·gallery 스킨 공통)
- 프론트: 403·404 에러 페이지가 컨트롤러가 넘긴 구체적 사유를 버리고 일반 문구만 보여주던 문제를 수정했습니다. "게시판을 찾을 수 없습니다", "글쓰기 권한이 없습니다" 처럼 무엇이 문제인지 화면에 나옵니다. 사유가 없으면 종전 문구 그대로입니다
- 이벤트: 이벤트 디스패처가 부모 클래스·인터페이스 이름으로 등록한 리스너를 실행하지 않던 문제를 수정했습니다. 세분화된 서브클래스로 발행되는 이벤트를 상위 이름으로 구독하면 조용히 무시됐고, 예외가 아니라서 로그에도 남지 않았습니다
- MemberPoint: 위 문제로 지급되지 않던 가입 축하 포인트가 정상 지급됩니다
- Shop: 위 문제로 발행되지 않던 LEVEL 트리거 쿠폰 자동발행이 정상 동작합니다
- Manual: 게시판 매뉴얼이 Board 스킨의 `basic` 폴백 동작을 실제와 반대로 설명하던 문제를 수정했습니다. 누락된 파일은 `basic` 의 같은 파일로 대체되며, 다만 그 화면만 톤이 어긋나므로 네 파일을 모두 두는 것을 권장합니다. 스킨 제작 가이드의 Board 스킨 파일 목록에서 빠져 있던 `Password.php` 도 추가했습니다
- Manual: 스킨 제작 가이드에 버전 표식이 없어, 번들 내용을 고쳐도 이미 가져간 도메인에는 전달되지 않던 문제를 수정했습니다. 가져오기를 다시 실행하면 개정본이 반영됩니다
- SnsLogin: 회원 탈퇴 시 SNS 제공자 연결 해제를 탈퇴가 확정된 뒤에 시도합니다. 이전에는 탈퇴 처리 전에 외부 연결을 먼저 끊어서, 이어지는 탈퇴 처리가 실패하면 계정은 그대로인데 SNS 로그인만 끊긴 상태로 남았고 되돌릴 방법이 없었습니다. SNS로 가입해 다른 로그인 수단이 없는 회원은 계정 접근 자체를 잃을 수 있었습니다
- SnsLogin: 제공자 연결 해제 실패가 더 이상 탈퇴를 막지 않습니다. 이전에는 제공자 장애나 관리자의 Client ID 삭제만으로 해당 회원이 탈퇴 자체를 할 수 없었고, 연결이 여럿일 때 중간에 실패하면 앞서 끊긴 연결은 방치됐습니다. 실패한 연결은 '폐기 실패'로 표시해 남기고, 관리자가 SNS 연동 내역에서 재시도할 수 있습니다
- SnsLogin: 관리자가 회원을 삭제할 때도 제공자 연결을 폐기합니다. 이전에는 로컬 연동 기록만 사라지고 제공자 측 연결과 토큰은 그대로 남아 추적할 수 없었습니다
- 확장: 일부 확장의 버전 표기가 실제와 달라 관리자 화면에 잘못 표시되던 것을 바로잡았습니다

## [1.0.0] - 2026-08-04

### Added
- Core: 멀티 도메인 컨텍스트 시스템, DI 컨테이너, 이벤트 디스패처, Contract 시스템
- Core: 라우팅, 인증/세션, 데이터베이스 추상화, 블록 기반 페이지 빌더
- Core: 웹 설치기 (6단계 — 환경 체크, DB, 도메인, 보안, 관리자, 완료)
- Core: 관리자 대시보드 및 사이트 운영 기능
- Core: 확장 호환성 검사·서명 검증·마이그레이션 체크섬·데이터 초기화 계약
- Core: 시작 킷, 사이트맵, 알림, 정책 개정 이력, 블록 개정 이력 및 복구 기능
- Package: Board — 게시판, 댓글, 카테고리, 리액션, 포인트 연동
- Package: Shop — 상품, 장바구니, 주문, 결제, 쿠폰, 배송, 리뷰
- Plugin(Board): BoardReport — 게시글 신고 접수와 블라인드 처리. 패키지 종속 플러그인의 레퍼런스 구현
- Plugin: VisitorStats — 서버사이드 방문자 통계, 대시보드 위젯
- Plugin: MemberPoint — 회원 포인트 적립/차감, 이벤트 기반 자동 지급
- Plugin: Banner — 이미지 배너 관리, 스케줄 표시, 블록 연동
- Plugin: Widget — 고정 위치 위젯 (PC 좌/우, 모바일 하단)
- Plugin: Popup — 레이어 팝업 (이미지/HTML, 반응형, 디바이스별 표시)
- Plugin: Survey — 설문 작성/수집/결과 집계, 블록 연동
- Plugin: Faq — FAQ 카테고리/항목 관리
- Plugin: Qna — Q&A 질문과 답변 관리
- Plugin: SnsLogin — 소셜 로그인 (네이버, 카카오, 구글)
- Plugin: Manual — 관리자·프론트 매뉴얼 책/페이지와 블록 콘텐츠
- Plugin: TestPay — 개발·테스트용 가상 결제. 모든 결제가 즉시 성공한다
- Plugin: PayApp — 페이앱 결제 연동 (카드/휴대폰/카카오페이/네이버페이/가상계좌)
- Plugin: EmailNotify — 코어 Mailer 기반 이메일 알림 발송, 도메인별 템플릿 관리
- Plugin: SendonSms — 센드온 SMS/LMS/MMS 발송 (도메인별 API 연동)
- Plugin: SendonTalk — 센드온 API 기반 카카오 알림톡 발송

[Unreleased]: https://github.com/wwiz-mublo/mublo/compare/v1.2.0...HEAD
[1.2.0]: https://github.com/wwiz-mublo/mublo/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/wwiz-mublo/mublo/compare/v1.0.1...v1.1.0
[1.0.1]: https://github.com/wwiz-mublo/mublo/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/wwiz-mublo/mublo/releases/tag/v1.0.0
